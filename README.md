# lsde2021-assignment1

## Assignments 1a and 1b

To build and run the `cruncher` binary on SF100, use:

```bash
make cruncher
./cruncher /opt/lsde/dataset-sf100-bidirectional-bin/ queries-test.csv out.csv
# in a separate terminal
tail -f out.csv
```

## Assignment 1c

See the `spark/` directory.
